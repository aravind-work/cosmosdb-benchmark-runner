benchmark {
    cosmosdb {
      //cosmos related configs
      serviceEndpoint: "https://moderakh-perf-java.documents.azure.com:443/"
      dbName: "adobe-test"
      masterKey: "XXX"
      maxPoolSize = 10000
      connectionMode = "Direct"
      consistencyLevel = "Eventual"
      requestTimeoutInMillis = 60000
      batchQueryTimeoutInMillis = 5000
      batchQueryExecutorThreadPoolSize = 1000
    }
}
